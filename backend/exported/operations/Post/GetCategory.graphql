query MyQuery($take: Int = 10, $skip: Int = 0) {
  data:main_findManycase_category(skip: $skip, take: $take) {
    id
    name
    case_post {
      author
      content
      id
      published_at
      poster
      title
    }
  }
}