query GetManyMenu @rbac(requireMatchAny: [admin]) {
  data: main_findManyadmin_menu {
    id
    parentId
    label
    path
    icon
    sort
    level
    is_bottom
    menu_type
    perms
    api_id
    create_time
  }
}