query MyQuery($take: Int = 10, $skip: Int = 0) {
  main_findManyadmin_category(skip: $skip, take: $take) {
    id
    name
    admin_post {
      author
      content
      id
      published_at
      poster
      title
    }
  }
}