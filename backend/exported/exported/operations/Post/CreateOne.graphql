mutation CreateOnePost($title: String!, $poster: String, $content: String, $publishedAt: DateTime, $username: String!, $userId: Int !, $cateId: Int !) @rbac(requireMatchAny: [user, admin]) {
  data: main_createOneadmin_post(
    data: {title: $title, poster: $poster, published_at: $publishedAt, author: $username, content: $content, user: {connect: {id: $userId}}, admin_category: {connect: {id: $cateId}}}
  ) {
    id
    title
    poster
    published_at
    author
    admin_category {
      id
    }
  }
}