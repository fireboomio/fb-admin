query MyQuery($equals: String!) @rbac(requireMatchAny: [admin]) {
  main_findManyadmin_category(where: {name: {equals: $equals}}) {
    admin_post {
      author
      cate
      content
      poster
      id
      title
      published_at
    }
  }
}